import React from "react";

const NotFoundPage = () => (
  <div>
    <h1>This page doesn't seem to exist</h1>
    <h2>Congrats, you found a hidden 404 page!</h2>
  </div>
);

export default NotFoundPage;
